package mango

func CSVToParquet(csvFilepath, parquetFilepath string) error {
	// reader, err := reader.NewParquetReader(csvFilepath)
	// if err != nil {
	// 	return fmt.Errorf("Couldn't create parquet reader: %f", err)
	// }
	// parquet := reader.Read()
	return nil
}
